# CLAUDE.md — Bridge to AGENTS.md

The **source of truth** for this repository's instructions is [`AGENTS.md`](./AGENTS.md).

Read [`AGENTS.md`](./AGENTS.md) **before doing anything**. It is an *always-on* index that points
to the context layer in [`ai-context/`](ai-context/) — including
[`ai-context/ARCHITECTURE.md`](ai-context/ARCHITECTURE.md),
[`ai-context/CONVENTIONS.md`](ai-context/CONVENTIONS.md), and the detailed rules in
[`ai-context/rules/`](ai-context/rules/), which must be read on the **first interaction of the
session**.

This file exists only to make sure an agent that reads this filename by convention (`CLAUDE.md`,
`AGENTS.md`, `GEMINI.md`, `.cursorrules`, `.windsurfrules`, `copilot-instructions.md`) is
redirected to the source of truth. **Do not duplicate rules here.**
